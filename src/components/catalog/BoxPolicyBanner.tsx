export function BoxPolicyBanner() {
  return (
    <div className="mb-6 flex items-center gap-3 border-l-2 border-signal bg-signal-100/50 px-4 py-3 text-sm text-ink-soft">
      <span className="font-mono-tab text-[11px] font-semibold uppercase tracking-wide text-signal-600">
        Box only
      </span>
      <span>
        Every style ships in fixed 8, 10, or 12-pair pre-pack boxes (EU 40–45) — single pairs aren&rsquo;t sold
        wholesale.
      </span>
    </div>
  );
}
